{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream --parallel",
    "test": "yarn jest --coverage"
  },
  "devDependencies": {
    "eslint": "^7.20.0",
    "jest": "^26.6.3",
    "lerna": "^3.22.1",
    "typescript": "^4.1.5"
  },
  "dependencies": {}
}
