{
  "name": "@lauf/lauf-runner-primitives",
  "version": "0.1.0-beta.1",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "check": "tsc --noEmit",
    "build": "tsc --build ./tsconfig.build.json",
    "prepare": "yarn run test && yarn run build"
  },
  "devDependencies": {
    "typescript": "^4.2.2"
  },
  "files": [
    "README.md",
    "dist"
  ],
  "dependencies": {
    "@lauf/lauf-queue": "^0.1.0-beta.1",
    "@lauf/lauf-runner": "^0.1.0-beta.3",
    "@lauf/lauf-store": "^0.1.0-beta.1"
  }
}