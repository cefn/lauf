{
  "name": "@lauf/lauf-store-react",
  "version": "0.1.0-beta.4",
  "main": "dist/index.js",
  "files": [
    "README.md",
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "check": "tsc --noEmit",
    "build": "tsc --build ./tsconfig.build.json",
    "prepare": "yarn run test && yarn run build"
  },
  "dependencies": {
    "@lauf/lauf-store": "^0.1.0-beta.4"
  },
  "peerDependencies": {
    "react": "^17.0.1"
  },
  "devDependencies": {
    "@testing-library/react": "^11.2.5",
    "typescript": "^4.2.2"
  }
}
