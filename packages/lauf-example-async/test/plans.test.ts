import {
  mainScript,
  fetchScript,
  focusSelector,
  focusedCacheSelector,
  triggerFocus,
  triggerFetchFocused,
} from "../src/plans";

describe("Test domain logic", () => {
  describe("Main script", () => {});

  describe("Fetch script", () => {});
});
